{
  "extends": ["//"],
  "tasks": {
    "generate": {
      "outputs": ["generated/**"]
    },
    "build": {
      "dependsOn": ["^generate"],
      "env": [
        "SUPABASE_URL",
        "SUPABASE_KEY",
        "DATABASE_URL",
        "JWT_SECRET"
      ]
    }
  }
}
